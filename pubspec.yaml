name: mappy2
description: Mappy 2.0, 16 months later

publish_to: "none"
version: 0.0.1+1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Flutter Mapbox GL + location
  mapbox_gl: ^0.15.0
  location: ^4.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true
